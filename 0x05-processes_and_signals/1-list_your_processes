#!/usr/bin/env bash
# This script displays all running processes in a user-oriented format with hierarchy

# Show all processes with user, PID, CPU/memory usage, and command
ps -eo user,pid,ppid,%cpu,%mem,tty,stat,cmd --forest
